#!/bin/env bash

# terminal emulator for dwm
term="alacritty --config-file $HOME/.dwm/alacritty/alacritty.yml"

if [[ $1 == "zsh" ]]; then
    $term -e zsh
elif [[ $1 == "fish" ]]; then
    $term -e fish
else
    $term
fi
